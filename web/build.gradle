plugins {
  id "com.moowork.node" version "1.3.1"
}

task bundleWeb(type: NpmTask) {
  args = ['run', 'webpack', '--', '-o', 'app.bundle.js', '--mode', 'development']
}

task runWeb(type: NpmTask) {
  args = ['run', 'serve']
}